Input — single-line text field, 34px tall by default. Pass `label`/`hint`/`error` for a fully-composed labeled field; omit them to get the bare input.

```jsx
<Input label="Webhook URL" placeholder="https://yourapp.com/webhooks/outpay" />
<Input label="Wallet address" error="Not a valid Base address" />
```
